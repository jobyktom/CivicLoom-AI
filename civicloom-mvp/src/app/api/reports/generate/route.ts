import { NextRequest, NextResponse } from "next/server";
import { getCensusMetrics } from "@/lib/census";
import { calculateOpportunityScore } from "@/lib/scoring";
import { demoReport } from "@/lib/mock-data";
import { getAdminSupabase } from "@/lib/supabase";
export async function POST(request:NextRequest){try{const body=await request.json();const [city,stateName]=String(body.location||"").split(",").map((x:string)=>x.trim());const state=stateCodes[stateName?.toUpperCase()]||stateCodes[stateName]||"48";let metrics=null;try{const geo=await fetch(`https://geocoding.geo.census.gov/geocoder/locations/onelineaddress?format=json&benchmark=Public_AR_Current&address=${encodeURIComponent(`${city}, ${stateName||""}`)}`).then(r=>r.json());const match=geo.result?.addressMatches?.[0];const county=match?.geographies?.Counties?.[0]?.GEOID?.slice(2);metrics=county?await getCensusMetrics({state,county}):null;}catch{}const base={...demoReport,id:crypto.randomUUID(),businessType:body.businessType||demoReport.businessType,locationName:body.location||demoReport.locationName,targetCustomer:body.targetCustomer||demoReport.targetCustomer,radius:Number(body.radius)||3,reportType:body.reportType||demoReport.reportType,createdAt:new Date().toISOString().slice(0,10),metrics:metrics||demoReport.metrics};const report={...base,opportunityScore:calculateOpportunityScore(base.metrics,base.businessType,base.targetCustomer)};const db=getAdminSupabase();if(db&&body.userId){await db.from("reports").insert({id:report.id,user_id:body.userId,business_type:report.businessType,location_name:report.locationName,geography_type:"county",state_code:state,radius:report.radius,target_customer:report.targetCustomer,opportunity_score:report.opportunityScore,ai_summary:report.aiSummary,risk_summary:report.riskSummary,recommendation:report.recommendation});await db.from("census_metrics").insert(Object.entries(report.metrics).map(([metric_name,metric_value])=>({report_id:report.id,metric_name,metric_value,source_year:2023})));}return NextResponse.json({report,source:metrics?"census":"demo"});}catch{return NextResponse.json({error:"Unable to generate report."},{status:500});}}
const stateCodes:Record<string,string>={"TEXAS":"48",TX:"48","CALIFORNIA":"06",CA:"06","NEW YORK":"36",NY:"36","NORTH CAROLINA":"37",NC:"37",ARIZONA:"04",AZ:"04",FLORIDA:"12",FL:"12",ILLINOIS:"17",IL:"17"};
