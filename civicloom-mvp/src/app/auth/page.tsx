"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { getBrowserSupabase } from "@/lib/supabase";
export default function Auth(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const supabase=getBrowserSupabase();if(!supabase){setMessage("Add Supabase credentials to enable sign-in. Demo mode is ready without them.");return}setLoading(true);const {error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error)setMessage(error.message);else router.push("/dashboard")}return <main className="grid min-h-[calc(100vh-140px)] place-items-center bg-[#f7f9fe] px-6 py-12"><form onSubmit={submit} className="w-full max-w-md rounded-[22px] border bg-white p-8 shadow-xl shadow-blue-950/5"><p className="text-sm font-bold uppercase tracking-[.18em] text-[#1769ff]">Welcome back</p><h1 className="mt-2 text-3xl font-semibold">Sign in to CivicLoom</h1><p className="mt-2 text-sm text-slate-500">Use your Supabase account to save and manage your reports.</p><label className="mt-7 block text-sm font-medium">Email<Input className="mt-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="mt-5 block text-sm font-medium">Password<Input className="mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{message&&<p className="mt-4 rounded-lg bg-blue-50 p-3 text-sm text-blue-800">{message}</p>}<Button disabled={loading} className="mt-6 w-full brand-gradient">{loading?"Signing in…":"Sign in"}</Button></form></main>}
