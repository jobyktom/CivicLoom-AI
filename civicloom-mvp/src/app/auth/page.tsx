"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input";
export default function Auth(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");const router=useRouter();function submit(e:React.FormEvent){e.preventDefault();setMessage("Hostinger MySQL is now the report database. Full email/password auth is the next step; for this MVP you can continue to the dashboard.");}return <main className="grid min-h-[calc(100vh-140px)] place-items-center bg-[#f7f9fe] px-6 py-12"><form onSubmit={submit} className="w-full max-w-md rounded-[22px] border bg-white p-8 shadow-xl shadow-blue-950/5"><p className="text-sm font-bold uppercase tracking-[.18em] text-[#1769ff]">Welcome back</p><h1 className="mt-2 text-3xl font-semibold">Sign in to CivicLoom</h1><p className="mt-2 text-sm text-slate-500">Supabase has been removed. Reports now save to Hostinger MySQL when database variables are configured.</p><label className="mt-7 block text-sm font-medium">Email<Input className="mt-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="mt-5 block text-sm font-medium">Password<Input className="mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{message&&<p className="mt-4 rounded-lg bg-blue-50 p-3 text-sm text-blue-800">{message}</p>}<Button className="mt-6 w-full brand-gradient">Check sign in</Button><Button type="button" variant="outline" className="mt-3 w-full" onClick={()=>router.push("/dashboard")}>Continue to dashboard</Button></form></main>}
