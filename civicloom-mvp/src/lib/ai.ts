import OpenAI from "openai";
import { Report } from "./types";
export async function generateReportNarrative(report:Report) { if(!process.env.OPENAI_API_KEY) return { executiveSummary:report.aiSummary, risks:report.riskSummary, recommendation:report.recommendation, marketingAngle:"Become the neighborhood's dependable daily ritual." }; const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY}); const result=await client.chat.completions.create({model:"gpt-4o-mini",messages:[{role:"system",content:"You are a practical local-market analyst. Be concise, direct, and evidence-aware."},{role:"user",content:`Assess ${report.businessType} for ${report.locationName}. Score ${report.opportunityScore}. Metrics: ${JSON.stringify(report.metrics)}. Return executive summary, risks, recommendation and marketing angle.`}]}); return {executiveSummary:result.choices[0].message.content??report.aiSummary,risks:report.riskSummary,recommendation:report.recommendation,marketingAngle:"Build a practical local habit, not just a brand."}; }
