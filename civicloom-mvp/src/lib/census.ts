import { CensusMetrics } from "./types";
const variables = { population:"B01003_001E", medianIncome:"B19013_001E", medianAge:"B01002_001E", housingUnits:"B25001_001E", owner:"B25003_002E", renter:"B25003_003E", employed:"B23025_004E", laborForce:"B23025_003E", bachelor:"B15003_022E" };
export async function getCensusMetrics({state,place,county}:{state:string;place?:string;county?:string}):Promise<CensusMetrics|null> { try { const geo=place?`for=place:${place}&in=state:${state}`:`for=county:${county}&in=state:${state}`; const key=process.env.CENSUS_API_KEY?`&key=${process.env.CENSUS_API_KEY}`:""; const response=await fetch(`https://api.census.gov/data/2023/acs/acs5?get=NAME,${Object.values(variables).join(",")}&${geo}${key}`,{next:{revalidate:86400}}); if(!response.ok) return null; const [headers,row]=await response.json(); const data=Object.fromEntries(headers.map((h:string,i:number)=>[h,row[i]])); const n=(v:string)=>Number(data[v])||0; const occ=n(variables.owner)+n(variables.renter); return {population:n(variables.population),medianIncome:n(variables.medianIncome),medianAge:n(variables.medianAge),housingUnits:n(variables.housingUnits),ownerPct:occ?n(variables.owner)/occ*100:0,employedPct:n(variables.laborForce)?n(variables.employed)/n(variables.laborForce)*100:0,bachelorPct:n(variables.population)?n(variables.bachelor)/n(variables.population)*100:0,commuteDrivePct:0}; } catch { return null; } }
export { variables as censusVariables };
